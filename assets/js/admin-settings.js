/**
 * Controls Yelp Widget Pro admin settings.
 */

jQuery( function() {

	// Tooltips for admins
	jQuery( '.tooltip-info' ).tipsy( {
		fade: false,
		html: true,
		gravity: 's',
		delayOut: 0,
		delayIn: 0
	} );

} );